# 1

I am very concerned by this section content idea and that discussion should be treated differently. The whole point of this clean Node domain was that regardless of the text structure, Preamble, Theorem, Remarks, Theorem Proof or whatever..., There is a simple structure where each content belongs to a Node.

What is the point of existing_node_content in Option A?

I am leaning option B. Would it be cleaner for Haiku to just have an output schema of a list of new nodes? The parent ids can be new nodes or existing nodes.

Let's carefully critique.

Let's consider if we need to make any changes re Q3.

Some considerations on the insturctions.

Do we need to do anything on the instruction issue? Maybe in the prompt? Maybe have two .md files in the state raw_read and raw_undread which is essentially the raw file but split into what it has and has not read. It has to option to read the previously read file if it needs to? Or maybe encourage a command/task so it only opens the file up to a line number.
